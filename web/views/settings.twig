{% extends 'base.twig' %}

{% set title='Settings' %}

{% block body %}

<div class="input-group">
    <div class="btn-group" role="group">
        <a href="changepassword" class="btn btn-default" aria-label="change password">
            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Change Password
        </a>
    </div>
</div>
<div class="input-group"> 
    <div class="btn-group" role="group">
        <a href="" class="btn btn-default" aria-label="change email address">
            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Change Email Address
        </a>
    </div>
</div>

{% endblock body %}
